{
  "framework": "nextjs",
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "env": {
    "NEXT_PUBLIC_API_URL": "https://api.city-maps.online"
  },
  "rewrites": [
    { "source": "/site/:path*", "destination": "https://api.city-maps.online/api/preview/:path*" }
  ]
}
